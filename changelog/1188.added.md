Columns are now converted to the type their contents imply before validation, using [skrub](https://skrub-data.org), which becomes a dependency. Numeric and categorical columns are passed through unchanged, so categorical detection and encoding are unaffected. The fitted converter is available on the estimator as `input_converter_`.
