Add SafeTensors checkpoint loading. TabPFN can now load model checkpoints from `.safetensors` files in addition to the legacy `.ckpt` format, with non-tensor metadata (architecture name, model config, inference config) embedded in the safetensors header.
