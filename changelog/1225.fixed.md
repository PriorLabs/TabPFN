Fitting or predicting on a DataFrame with a genuine `datetime64` column alongside any numeric, boolean, or string column now raises a `TabPFNUserError` naming the column, instead of failing validation with numpy's dtype-promotion error.
