Date detection now only recognizes columns with a genuine `datetime64` dtype, rather than also trying to parse every string/object column with `pd.to_datetime()`. Such a column is refused with a `TabPFNValidationError` naming it, since nothing expands a date into features; cast or expand it yourself before fitting. The never-consumed `FeatureModality.DATE` has been removed.
