Date detection now only recognizes columns with a genuine `datetime64` dtype, rather than also trying to parse every string/object column with `pd.to_datetime()`. Such a column is read as a plain numeric timestamp (nanoseconds since the epoch) instead of being ordinal-encoded as a high-cardinality category, and the never-consumed `FeatureModality.DATE` has been removed.
