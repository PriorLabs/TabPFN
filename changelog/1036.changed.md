Removed the `per_feature` option from `PreprocessorConfig.name`. It was never functional: every fit crashed on construction since the initial public release, so no working configuration can depend on it.
