Regression prediction runs the bar-distribution border remap in float32 and repeats it in float64 only when the faster pass leaves a bucket empty, so a well-behaved target pays little for the precision a spiky one needs. Predictions for targets that need float64 are unchanged; the rest move by float32 round-off.
