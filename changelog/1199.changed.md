Regression target transforms now act on the target in its original units, before standardization. This fixes `1_plus_log` producing non-finite model inputs for positive targets whose z-scores fall below -1. Predictions can change for ensembles using non-identity target transforms, including `safepower`; `None` and `"none"` retain standardization only.
