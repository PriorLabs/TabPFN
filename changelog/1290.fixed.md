Fixed `FinetunedTabPFNRegressor` computing the loss against z-scored targets for ensemble members that use a target transform (such as the default `safepower`); their loss now uses the transformed targets they predict in.
