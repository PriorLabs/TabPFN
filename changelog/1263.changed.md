Faster preprocessing of tables with categorical columns: the ordinal encoding step, which runs once per ensemble member, is about three times cheaper on small tables. Preprocessed outputs are unchanged.
