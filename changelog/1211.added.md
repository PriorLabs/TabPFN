`fit()` can now expand a detected free-text column into numeric features via `skrub.StringEncoder`, behind a new `InferenceConfig.USE_TEXT` flag that defaults to `False`. With the flag off, behavior is unchanged: a text column is still ordinal-encoded as a high-cardinality category and `fit()` warns about it by name.
