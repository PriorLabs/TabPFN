A `fit_mode="fit_with_cache"` estimator no longer keeps each ensemble member's preprocessed copy of the training features after building its KV caches, which predict never reads. This frees roughly `n_estimators` times the training set in host memory for the life of the estimator, and shrinks saved fitted models by the same amount. Predictions are unchanged.
