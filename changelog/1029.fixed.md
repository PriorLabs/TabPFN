Fixed regressor ensemble members sharing a single mutable `target_transform` instance. With in-process preprocessing (`n_preprocessing_jobs=1`), each member's in-place fit clobbered the fitted state of the others, silently corrupting predictions whenever members were fitted on different targets (e.g. with row subsampling active). Each ensemble config now owns a deep copy of the transform.
