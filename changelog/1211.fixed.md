A string column listed in `categorical_features_indices` is now read as categorical whatever its number of distinct values. Above `MIN_CARDINALITY_FOR_TEXT` it used to be classified as text, silently, since declaring it also suppressed the free-text warning, and so was neither cast to category nor passed to the model as categorical.
