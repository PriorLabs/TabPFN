Regression ensembles now normalize each member’s target using its own training rows, correcting predictions when row subsampling is enabled. Without row subsampling, predictions may differ slightly due to floating-point rounding.
