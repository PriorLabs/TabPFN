Saving a fitted estimator that was given a `torch.device` (or a sequence of them) as its `device` raised `TypeError: Object of type device is not JSON serializable`, since the init params are JSON-dumped and only `torch.dtype` was rendered to a string. Device specs are now rendered too.
