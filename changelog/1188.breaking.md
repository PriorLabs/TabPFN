`scikit-learn>=1.4.2`, `pandas>=1.5.3` and `numpy>=1.23.5` are now the minimum supported versions, raised to match the new `skrub` dependency. Note also that `n_features_in_` and `feature_names_in_` describe the converted frame, so they grow when a datetime column is expanded into calendar features.
