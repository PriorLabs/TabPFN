Deprecate `ClassifierModelSpecs` and `RegressorModelSpecs` in favor of `ModelSpecs`. Existing constructors remain supported; select tasks through the estimator rather than specs subclass checks. See the in-memory model migration guide.
