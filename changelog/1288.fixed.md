Regression density and likelihood evaluation no longer reports infinite NLL for targets that fall in a narrow bucket or in a distribution's tail; point predictions and CRPS are unchanged. With `eval_metric="nll"`, temperature tuning previously discarded every candidate whose holdout loss came out infinite and silently kept the default temperature, so a tuned NLL temperature may now differ.
