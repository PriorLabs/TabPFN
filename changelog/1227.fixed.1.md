Fitting or predicting on a `timedelta64` column no longer aborts: a duration becomes its length in seconds. A `period` column is handled like a `datetime64` one, refused with a clear error or expanded with `TRANSFORM_DATES`, instead of aborting with `Unknown dtype: period[D]`.
