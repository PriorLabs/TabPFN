`TabPFNRegressor` no longer fails or returns meaningless predictions when the target's mean is large relative to its spread (e.g. ID-like values around `1e10`). Point and quantile predictions are now returned as `float64` instead of `float32`, and the `raw_space_bardist_` / `output_type="full"` criterion is `float64` on every device except MPS.
