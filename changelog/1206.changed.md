`FeatureModality.DATE` was removed. A datetime column is either expanded into numerical calendar features or read as a plain category or text before the feature schema is built, so no feature was ever tagged with it.
