TabPFN-3.5 with `inference_precision=torch.float16` on CPU no longer returns uniform predictions on torch 2.5 to 2.8: the RMSNorm and the scaled attention run in fp32 for fp16 inputs on CPU, where the fp16 kernels overflowed.
