Fitting on a dataframe containing a `datetime64` column no longer raises. Depending on the other columns it previously failed with a dtype promotion error during input validation, with `ValueError: Invalid dtype for X` while cleaning, or with a promotion error inside the ordinal encoder.
