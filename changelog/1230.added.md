`InferenceConfig.N_ESTIMATORS` lets a checkpoint declare how many estimators to run; `n_estimators="auto"` now takes that count instead of a hard-coded 8. Checkpoints without the field fall back to the same 8, so nothing changes for them. An explicit `n_estimators` still wins; naming a count both ways, or ensembling checkpoints that disagree on it, raises.
