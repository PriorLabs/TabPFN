`n_preprocessing_jobs > 1` now parallelises the per-estimator preprocessing over threads instead of processes, so it speeds up the fit instead of slowing it down. The default stays 1 and predictions are unchanged.
