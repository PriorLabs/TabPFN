`n_preprocessing_jobs > 1` now parallelises the per-estimator preprocessing over threads instead of processes, which makes it faster than `n_preprocessing_jobs=1` at every table size (about 2x on 1M rows) instead of slower. The default stays 1 and predictions are unchanged.
