Chunk large test sets during cached (`fit_mode="fit_with_cache"`) inference to bound peak GPU memory, controlled by the new `TABPFN_MAX_BATCHED_TEST_ROWS` setting (default `32768`; set to `0` to disable). Chunking is mathematically equivalent.
