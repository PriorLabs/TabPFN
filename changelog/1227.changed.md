The out-of-memory error now reports the feature count TabPFN actually ran on rather than the width of the frame passed to `fit`, which can be narrower when date expansion is on. `TabPFNRegressor` reports the count at all now, having previously omitted it.
