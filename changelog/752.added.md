- The estimators' `fit()` functions now only allow the types `pd.DataFrame` and `np.ndarray`
- The classifier gets a dedicated method for fitting with differentiable input called `fit_with_differentiable_input()`
