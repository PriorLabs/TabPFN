Add a shared `ModelSpecs` dataclass for in-memory classification and regression. Regression automatically derives its distribution from `model.regression_borders`; legacy models without embedded borders still require `norm_criterion`.
