Fix `TabPFNRegressor` rejecting a checkpoint whose config also describes a classification head, with `was expected to have a FullSupportBarDistribution criterion, but instead had a CrossEntropyLoss criterion`. The criterion now follows the task the estimator is being built for instead of being inferred from `max_num_classes`, and the regression bucket borders are read from wherever the checkpoint keeps them. Loading a regression checkpoint into `TabPFNClassifier` now raises instead of silently building a bar distribution it discards.
