Fix `TabPFNRegressor` rejecting a checkpoint whose config also describes a classification head: the criterion now follows the task the estimator is built for rather than being inferred from `max_num_classes`. Loading a regression checkpoint into `TabPFNClassifier` now raises instead of silently building an unused bar distribution.
