Building a model from a checkpoint no longer runs torch's random parameter initialisation before the weights are loaded. That initialisation was most of the per-fit model construction time for the TabPFN-3 and TabPFN-3.5 checkpoints. The loaded model is unchanged.
