TabPFN-3.5 no longer over-extrapolates past the top or bottom of a feature whose
values repeat, such as an integer year column with one row per month. Values
outside the train range now take the rank of the nearest train extreme.
