Add automatic model routing: `TabPFNClassifier` and `TabPFNRegressor` now select TabPFN v3 for datasets with fewer than 250 features or more than 100,000 samples, and v2.6 otherwise, when `model_path="auto"` (the default).
