Support nested inference_config__FIELD parameters on TabPFNClassifier and TabPFNRegressor so single inference settings can be tuned via set_params and GridSearchCV without replacing the whole inference_config dict.
