Add `kv_cache_precision="adaptive"`: the KV cache is stored on the grid an attention backend declaring `kv_grid_dtype` already rounded the keys and values to, when that backend took the call, and as `"int8"` otherwise.
