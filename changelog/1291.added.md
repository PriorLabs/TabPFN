Add a FlashAttention-4 attention backend (`pip install "tabpfn[fa4]"`, or `[fa4-cu13]` on CUDA 13) covering Hopper and Blackwell GPUs. It dispatches automatically for fp16 attention at any sequence length and also serves the head_dim-16 feature-attention stages. Measured on TabPFN v3 `predict()`: up to 1.30x on H100 and 1.26x on GB200 at 100k rows and above. bf16 stays on SDPA on Blackwell, with a one-time warning.
