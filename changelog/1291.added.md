Add a FlashAttention-4 attention backend for Hopper and Blackwell GPUs, installed with `pip install "tabpfn[fa4]"` (or `[fa4-cu13]` on CUDA 13) and used automatically for fp16 attention. bf16 stays on SDPA on Blackwell, with a one-time warning.
